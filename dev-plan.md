你是一名资深前端工程师与产品实现负责人，请根据以下需求直接输出可运行的网页应用代码与必要说明。要求界面简洁稳定，交互直白，生成数据用于测试。重点实现侧边栏配置、可选字段与排序、列表展示、点击复制、导出、多标签页、收藏与备注。本应用不得生成可真实使用的个人敏感标识，所有身份证号、手机号、邮政编码、地址必须为明确不可用的测试占位数据，且在界面与导出中标记为测试数据。

## 1. 页面与布局
- 单页应用，顶部为工具栏，主体为大列表。
- 采用 Tab 切换：
    - 生成页面
    - 收藏页面
- 生成页面包含可展开收起的侧边栏，默认展开。
- 侧边栏收起时保留一条窄边与展开按钮，并显示当前配置概览。

## 2. 侧边栏配置
侧边栏包含分组折叠面板，每组都有启用开关与重置。
- 性别
    - 选项：男、女、不指定
- 出生年月日
    - 模式：指定日期、指定范围、不指定
    - 指定日期：固定 YYYY-MM-DD
    - 指定范围：起止 YYYY-MM-DD
    - 允许只填年份或年月，自动补全为范围
- 省市区
    - 逐级选择：省、市、区
    - 每一级支持不指定
- 生成控制
    - 一次生成条数
    - 随机种子：同一配置+同一种子生成结果可复现
    - 导出是否包含表头
- 字段选择与顺序
    - 字段可勾选显示/隐藏
    - 字段顺序可拖拽调整

侧边栏底部固定按钮：
- 生成
- 清空列表
- 重置配置

## 3. 数据字段
每条记录包含以下字段，用户可选择生成哪些字段并调整顺序：
- 身份证号：测试占位，长度与展示格式符合身份证号外观，但必须保证不可通过校验，且不对应真实行政区
- 姓名：中文姓名
- 性别：与配置一致或随机
- 出生年月日：与配置一致或随机
- 省市区：与配置一致或随机
- 地址：测试占位地址，不对应真实道路与小区
- 邮政编码：测试占位编码，不对应真实投递区域
- 手机号：测试占位号码，不可接通，不使用真实可用号段
- 邮箱：统一使用示例域名 example.com，确保不可投递到真实用户
- 测试标记字段：固定为 test

## 4. 生成页面列表
- 主体为大列表，每行一条记录，每列一个字段。
- 支持生成多条并追加显示。
- 点击单元格即可复制该值到剪贴板，并有明确的复制成功提示。
- 支持复制整行：
    - 以当前字段顺序拼接
    - 支持 JSON 单条对象、CSV 单行、制表符分割单行

## 5. 导出与批量复制
- 右上角提供下拉框按钮，支持复制或导出：
    - JSON：数组形式与按对象键名输出
    - CSV：严格转义，分隔符使用逗号
    - Excel 制表符分割：\t 分割
- 支持选择是否包含表头
- 导出内容必须包含测试标记字段，避免误入生产数据

## 6. 收藏与备注
- 生成页面每行可一键收藏。
- 收藏页面展示收藏列表。
- 每条收藏支持备注编辑、取消收藏、点击复制单元格。
- 收藏支持搜索与筛选。
- 收藏数据使用本地存储持久化，并提供导出收藏为 JSON 的入口。

## 7. 技术要求
- 使用纯前端实现，可用 React 或原生方案，但必须提供完整可运行代码。
- 不依赖后端服务。
- 性能支持至少 5000 行的虚拟滚动或分页方案。
- 代码中写清楚目录结构、如何启动、主要组件划分与关键实现点。

请直接输出：
1) 项目目录结构
2) 关键文件完整代码
3) 启动运行步骤
4) 你做的取舍说明
